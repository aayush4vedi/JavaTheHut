<h1><%= title %></h1>

<form action="/create" method="POST">
    <input name="category[name]" value = "<%= category.name %>">
    <input name="category[cook]" value = "<%= category.cook %>">
    <input name="category[dish]" value = "<%= category.dish %>">

    <button type="submit">Submit</button>
</form>